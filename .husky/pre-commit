#!/bin/sh

# Format Rust code
if [ -f "Cargo.toml" ] && command -v cargo >/dev/null 2>&1; then
    echo "Running cargo fmt..."
    cargo fmt
    
    # Stage any formatting changes
    git add -u
fi

# Format markdown files on commit
npx lint-staged

# Run tests (when available)
# npm test